name: CI

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  check:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v5
        with:
          node-version: 24
          cache: npm

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy, rustfmt

      - name: Rust cache
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: src-tauri

      - name: Install dependencies
        run: npm ci

      - name: TypeScript type check
        run: npx tsc --noEmit

      - name: JS/TS lint
        run: npx oxlint

      - name: JS/TS format check
        run: npx oxfmt --check

      - name: Frontend tests
        run: npx vitest run

      - name: Single-file frontend build
        env:
          VITE_API_BASE: ""
        run: npm run build

      - name: Docker entrypoint tests
        run: sh script/docker-entrypoint.test.sh

      - name: Session cleanup script tests
        run: bash script/delete-codex-sessions.test.sh

      - name: Rust format check
        run: cargo fmt --manifest-path src-tauri/Cargo.toml --check

      - name: Rust lint
        run: cargo clippy --manifest-path src-tauri/Cargo.toml -- -D warnings

      - name: Rust tests
        run: cargo test --manifest-path src-tauri/Cargo.toml
